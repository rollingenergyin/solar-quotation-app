/** Effective solar year for ROI / generation (not calendar 365). */
export const ROI_DAYS_PER_YEAR = 300;
/** Days per month for converting average monthly consumption to average daily kWh. */
export const BILLING_DAYS_PER_MONTH = 25;
