# Always install devDependencies during npm install.
# Without this, NODE_ENV=production (set by Render during build) would skip
# devDependencies, leaving TypeScript and Prisma CLI unavailable at build time.
include=dev
